# Pinned: CI installs fresh 4x/day — an unpinned breaking release would take
# down ingestion, and the ~1-week RSS window makes slow recovery lossy.
# Bump deliberately, run scripts/fetch_and_build.py locally, then commit.
feedparser==6.0.12
vaderSentiment==3.3.2
python-dotenv==1.2.2
supabase==2.31.0
anthropic==0.109.0
